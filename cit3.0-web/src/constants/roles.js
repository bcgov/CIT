/**
 * Roles enum, provide the available role names that a user can belong to.
 */
const Roles = {
  SUPER_ADMINISTRATOR: "SuperAdministrator",
  SYSTEM_ADMINISTRATOR: "IDIR",
  ECONOMIC_DEVELOPMENT_OFFICER: "BCeID",
  POWER_BI_VIEWER: "PowerBI",
};

export default Roles;
