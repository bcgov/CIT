version: '3.3'

services:
  cit-web:
    command: "yarn build && yarn start"
    environment:
      - NODE_ENV=production

  nginx:
    ports:
      - 80:80
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - static:/static
      - ./cit-web/dist:/cit-web