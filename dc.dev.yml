version: '3.3'

services:
  cit-web:
    command: bash -c "yarn && yarn dev"
    environment:
      - NODE_ENV=development
      - POWERBI_REPORT_ID=3972090f-aa22-405d-ab75-2a26ab512010
    ports:
      - 8080:8080
  nginx:
    ports:
      - 80:80

